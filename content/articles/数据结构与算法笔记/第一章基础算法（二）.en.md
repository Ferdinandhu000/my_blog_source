---
title: Chapter I Foundation algorithm (ii)
slug: foundational-algorithms-principles-and-analysis-part-ii
date: '2025-07-01 10:15:02+08:00'
category: Algorithms & Data Structures
draft: false
tags:
- Algorithm Basis
- Course notes
---
## Prefix and

### What's a prefix and?
For original arraya [n]
a[1], a[2], a[3], a[4], ..., a[n]
Prefixes and arrays are the first n and the first n of s[n] arrays
s[1] = a[1]  
s[2] = a[1] + a[2] = s[1] + a[2]  
s[3] = a[1] + a[2] + a[3] = s[2] + a[3]  
...  
s[n] = a[1] + a[2] + a[3] + a[4] + ... + a[n] = s[n-1] + a[n]  
### Prefixes and effects
- Quick find the first n item of the array and, or the sum of a section between
- For example: a[n] first ten items and s [10], a [n] in a section [10,20] and s [20] - s[9]

### One-dimensional prefix and array
AcWing 795. Prefix
#### Title Description
Enter a length to$n$the integer sequence.
Enter next$m$A pair of questions.$l, r$。
For each query, output from the original sequence$l$Count to number.$r$Numbers and.

#### Input Format
First row contains two integers$n$and$m$。
Organisation$n$is an integer number representing an integer column.
Next$m$Line, two integers per row$l$and$r$, indicates the range of an inquiry.

#### Output Format
Total$m$Okay, output one query per line.

#### Data range
$1 \leq l \leq r \leq n$，
$1 \leq n, m \leq 100000$，
$-1000 \leq$Value of elements in columns$\leq 1000$

#### Enter a sample
```
5 3
2 1 3 6 4
1 2
1 3
2 4
```

#### Output Sample
```
3
6
10
``` 
Reference Code
```java
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        int n = in.nextInt();
        int q = in.nextInt();
        int[] arr = new int[n+1];
        for(int i = 1; i <= n; i++) {
            arr[i] = in.nextInt();
        }
        
        int[] s = new int[n+1];
        for(int i = 1; i <= n; i++) {
          // 生成前缀和数组s[n]
          // 这里用O(n)的时间复杂度生成前缀和数组，是为了方便之后查询所求前缀和(每次查询都为O(1))
            s[i] = s[i-1] + arr[i];
        }
        
        while(q-- > 0) {
            int l = in.nextInt();
            int r = in.nextInt();
            System.out.println(s[r] - s[l-1]);
        }
       
    } 
}
```
### 2D prefix and
AcWing 796. Sum of sub-forms
#### Title Description
Enter a length to$n$the integer sequence.
Enter next$m$A pair of questions.$l, r$。
For each query, output from the original sequence$l$Count to number.$r$Numbers and.

#### Input Format
First row contains two integers$n$and$m$。
Organisation$n$is an integer number representing an integer column.
Next$m$Line, two integers per row$l$and$r$, indicates the range of an inquiry.

#### Output Format
Total$m$Okay, output one query per line.

#### Data range
$1 \leq l \leq r \leq n$，
$1 \leq n, m \leq 100000$，
$-1000 \leq$Value of elements in columns$\leq 1000$

#### Enter a sample
```
5 3
2 1 3 6 4
1 2
1 3
2 4
```

#### Output Sample
```
3
6
10
```
Reference Code
```java
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        int n = in.nextInt();
        int m = in.nextInt();
        int q = in.nextInt();
        int[][] arr = new int[n+1][m+1];
        for(int i = 1; i <= n; i++) {
            for(int j = 1; j <= m; j++) {
                arr[i][j] = in.nextInt();
            }
        }
        
        // 生成前缀和矩阵s
        int[][] s = new int[n+1][m+1];
        for(int i = 1; i <= n; i++) {
            for(int j = 1; j <= m; j++) {
                // 二维前缀和矩阵公式
                s[i][j] = s[i-1][j] + s[i][j-1] - s[i-1][j-1] + arr[i][j];
            }
        }
        
        while(q-- > 0) {
            int x1 = in.nextInt();
            int y1 = in.nextInt();
            int x2 = in.nextInt();
            int y2 = in.nextInt();
            // 通过前缀和矩阵求得区间和，时间复杂度为O(1)
            int sum = s[x2][y2] - s[x2][y1-1] - s[x1-1][y2] + s[x1-1][y1-1];
            System.out.println(sum);
        }
    }

}
```

## Difference
### What's the difference?
Discrepancies are the reverses of the **prefix and arrays of the original array**, i.e. the differentials of the original array are prefixes and arrays of the original array.

### 1-D differential fraction group

AcWing 797. Difference
#### Title Description
Enter a length to$n$the integer sequence.
Next$m$Each operation contains three integers$l, r, c$, means that the sequence will be in$[l, r]$And each number between$c$。
Please output the sequence after all operations.

#### Input Format
First row contains two integers$n$and$m$。
Organisation$n$integer number, representing the whole number series.
Next$m$Line, each line contains three integers$l, r, c$, means an operation.

#### Output Format
One line, including$n$integer number, indicating the final sequence.

#### Data range
$1 \leq n, m \leq 100000$，
$1 \leq l \leq r \leq n$，
$-1000 \leq c \leq 1000$，
$-1000 \leq$Value of elements in integer series$\leq 1000$

#### Enter a sample
```
6 3
1 2 2 1 2 1
1 3 1
3 5 1
1 6 1
```

#### Output Sample
```
3 4 5 3 4 2
``` 

Reference Code
```java
// 算法逻辑：对于传入的原数组arr[], 先定义一个数组b[]作为arr的差分数组，因此，只需在差分数组上进行操作（例如需要将区间[l,r]上的元素全部加c，则只需在差分数组中的l位置添加c，r+1位置减c即可）, 再将b本身还原成其自己的前缀和数组即可
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        int n = in.nextInt();
        int q = in.nextInt();
        int[] arr = new int[n+1];
        for(int i = 1; i <= n; i++) {
            arr[i] = in.nextInt();
        }
        
        int[] b = new int[n+2];
        for(int i = 1; i <= n; i++) {
            b[i] = arr[i] - arr[i-1];
        }
        
        while(q-- > 0) {
            int l = in.nextInt();
            int r = in.nextInt();
            int c = in.nextInt();
            
            b[l] += c;
            b[r+1] -= c;
            
        }
        for(int i = 1; i <= n; i++) {
            b[i] = b[i-1] + b[i];
        }
        
        for(int i = 1; i <= n; i++) {
            System.out.print(b[i] + " ");
        }
    }
}
```
### 2D differential fractions
AcWing 798. 2D Matrix
#### Title Description
Enter one$n$Okay.$m$Integer matrix of columns, then entered$q$Each operation contains five integers$x_1, y_1, x_2, y_2, c$, where$(x_1, y_1)$and$(x_2, y_2)$The upper left and lower right coordinates of a sub-form, each operation adding the value of each element of the selected sub-form$c$I don't know. Please perform all operational matrix outputs.

#### Input Format
First row contains integers$n, m, q$ 。
Next$n$Lines, every line contains$m$an integer, representing the integer matrix.
Next$q$Line, each line contains 5 integers$x_1, y_1, x_2, y_2, c$, means an operation.

#### Output Format
Total$n$All right, every line.$m$Integers, representing the final matrix after all operations have been completed.

#### Data range
$1 \leq n, m \leq 1000$，
$1 \leq q \leq 100000$，
$1 \leq x_1 \leq x_2 \leq n$，
$1 \leq y_1 \leq y_2 \leq m$，
$-1000 \leq c \leq 1000$，
$-1000 \leq$Value of elements in the matrix$\leq 1000$ 

#### Enter a sample
```
3 4 3
1 2 2 1
3 2 2 1
1 1 1 1
1 1 2 2 1
1 3 2 3 2
3 1 3 4 1
```

#### Output Sample
```
2 3 4 1
4 3 4 1
2 2 2 2
``` 
Reference code,
```java
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        int n = in.nextInt();
        int m = in.nextInt();
        int q = in.nextInt();
        int[][] a = new int[n+2][m+2];
        for(int i = 1; i <= n; i++) {
            for(int j = 1; j <= m; j++) {
                a[i][j] = in.nextInt();
            }
        }
        
        int[][] b = new int[n+2][m+2];
        for(int i = 1; i <= n; i++) {
            for(int j = 1; j<= m; j++) {
                // 生成差分矩阵b
                // 注意此处差分矩阵的计算方式
                b[i][j] += a[i][j];
                b[i+1][j] -= a[i][j];
                b[i][j+1] -= a[i][j];
                b[i+1][j+1] += a[i][j];
            }
        }
        
        while(q-- > 0) {
            int x1 = in.nextInt();
            int y1 = in.nextInt();
            int x2 = in.nextInt();
            int y2 = in.nextInt();
            int c = in.nextInt();
            // 在差分矩阵中进行加减操作，直接查找，时间复杂度为O(1)
            // 注意此处的计算方式
            b[x1][y1] += c;
            b[x2+1][y1] -= c;
            b[x1][y2+1] -= c;
            b[x2+1][y2+1] += c;
        }
        
        // 最后将b矩阵还原成其本身的前缀和矩阵，即为最终结果
        // b[i][j]表示[1,1]到[i,j]围成的矩形内元素的和
        for(int i = 1; i <= n; i++) {
            for(int j = 1; j <= m; j++) {
                b[i][j] += b[i-1][j] + b[i][j-1] - b[i-1][j-1];
            }
        }
        for(int i = 1; i <= n; i++) {
            for(int j = 1; j <= m; j++) {
                System.out.print(b[i][j] + " ");
            }
            System.out.println("");
        }
    }
}
```