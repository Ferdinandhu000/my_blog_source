---
title: Programme design and algorithmic language end review3
slug: programming-and-algorithmic-language-comprehensive-review-part-iii
date: '2025-06-12 11:10:39+08:00'
category: Algorithms & Data Structures
draft: false
tags:
- Program design
- Review of courses
---
## It's a mistake.

### Python Block
1. Don't use it in your judgment.`/`To determine whether the two numbers are equal

2. Infinity and infinity in Python:
  ```python
  a = float('Inf')
  b = float('-Inf')
  ```

3. Initialize a two-dimensional array (matrix)
  ```python
  ls = [[0]*3 for i in range(3)]
  print(ls)
  ```
  or
  ```python
  ls = []
  for i in range(3):
      row = []
      for j in range(3):
          row.append(0)
      ls.append(row)
  print(ls)
  ```
  or
  ```python
  n = 3
  ls = [[0]*n] * n
  print(ls)
  ```
  
4. Bubble Sorting and Select Sorting
  ```python
  # 冒泡排序
  def bubble_sort(s):
      for i in range(len(s) - 1):  # 注意要-1
          for j in range(len(s) - i - 1):
              if s[j] > s[j + 1]:
                  s[j], s[j + 1] = s[j + 1], s[j]
      return s
  
  a = [0, 4, 12, 10, 7, 3, 9, 1, 5, 6]
  print(bubble_sort(a))
  
  # 选择排序
  def selection_sort(a):
      for i in range(len(a) - 1):
          min_index = i
          for j in range(i + 1, len(a)):
              if a[j] < a[min_index]:
                  min_index = j
          if min_index != i:
              a[i], a[min_index] = a[min_index], a[i]
      return a
  
  a = [0, 4, 12, 10, 7, 3, 9, 1, 5, 6]
  print(selection_sort(a))
  ```

5. ord() and chr()
  - ord: str -> ASCII
    ```python
    ord('a') == 97
    ```
  - chr: ASCII -> str
    ```python
    chr(ord('a') + 1) == 'b'
    ```
  - Caesar code:
    ```python
    new_char = chr((ord(char) - ord('A') + shift) % 26 + ord('A'))  # 大写字母
    ```

6. Sort() Method
  ```python
  s.sort()    # 而非sort(s)！！！
  ```

7. Fibonacci series
  ```python
  #             1  1  2  3  5  8 ...
  # 第一轮输出    a  b
  # 第二轮输出       a  b
  # 对应第一轮中参数 b a+b
  
  # 迭代算法
  def fibo(n):
      ls = []
      a,b = 0,1
      for i in range(n):
          a,b = b,a+b
          ls.append(a)
      return ls
  print(fibo(10))
  
  # 递归算法
  def Fibo(n):
      if n==0:
          return 0
      elif n==1:
          return 1
      else:
          return Fibo(n-1)+Fibo(n-2)
  for i in range(1,11):
      print(Fibo(i))
  ```

8. Input
  - Separate the string entered into the array ('abcde'-> ['a','b','c','d','e'])
    ```python
    a = input()
    ls = list(a)
    print(ls)
    ```
  - Separated a paragraph of the text entered into an array by space ("Hello world, how are you?"
    ```python
    text = input()
    print(text.split())  # split()方法默认按空格分隔，传入参数','也可指定按逗号分隔
    ```
  - Enter a group of numbers separated by commas, and type the number in the array instead of sstr
    ```python
    a = input().split(',')
    a = [int(num) for num in a]
    print(a)
    ```

9. Output
  - Center left-to-right
    ```python
    print('{:>10}'.format('hello'))  #      hello
    print('{:<10}'.format('hello'))  # hello  
    print('{:^10}'.format('hello'))  #   hello
    ```
  - Fill
    ```python
    print('{:*>10}'.format('hello'))  # *****hello
    print('{:*^10}'.format('hello'))  # **hello***
    ```
  - Digital processing
    ```python
    print("{:,}".format(1234567))  # 1,234,567
    print("{:.2f}".format(3.1415926))  # 3.14
    ```

10. Function Recursive (Hannota)
    ```python
    def Hanoi(n, A, B, C):
        # 当只有一个盘子时，直接将盘子从A移动到C
        if n == 1:
            print('Move {} from {} to {}'.format(n, A, C))
        else:
            # 步骤1：将上面的n-1个盘子从A借助C移动到B
            Hanoi(n-1, A, C, B)
            # 步骤2：将第n个盘子从A移动到C
            print('Move {} from {} to {}'.format(n, A, C))
            # 步骤3：将n-1个盘子从B借助A移动到C
            Hanoi(n-1, B, A, C)

    # 获取用户输入的盘子数量
    n = int(input('Please enter n here: '))
    # 调用函数解决汉诺塔问题
    Hanoi(n, 'A', 'B', 'C')
    ```
11. join() function
  join() is a method for connecting elements in an iterative object (e.g., list, metagroup, collection, etc.) into a ** string**
  ```python
  words = ["Hello", "World", "!"]
  result = " ".join(words)  # 使用空格作为分隔符
  print(result)  # 输出：Hello World !
  ```

12. Case conversion
  ```python
  def swapLowerUpper(s):
      res = list(s)
      for i in range(len(s)):
          if 'a' <= s[i] <= 'z':
              res[i] = chr(ord(s[i])-32)
      return ''.join(res)
  
  def swapUpperLower(s):
      res = list(s)
      for i in range(len(s)):
          if 'A' <= s[i] <= 'Z':
              res[i] = chr(ord(s[i])+32)
      return ''.join(res)

  a = input()
  print(swapLowerUpper(a))
  print(swapUpperLower(a))
  ```
  **Note: ** python supports an index of a string like s[0] but cannot be copied (e.g. s[0] = 'a ' ), so the string is converted to a list form first. Another way to create one.`result = []`, and then`for char in s`Act and use each character in the s in turn`result.append(char)`We'll need it for storage.`return ''.join(result)`Convert List to String Return

### Java Block

1. Format Output
  - Printf Output
    ```java
    double num = 1.000;
    System.out.printf("A number %.2f", a);
    ```
  - Spring.format output
    ```java
    double num = 1.000;
    System.out.println(String.format("A number %.2f", a));
    ```
  - Organisation
    ```java
    double num = 1.000;
    System.out.println("A number %.2f".formatted(a));
    ```
  Note: Formatted output of time`String.format("%02d:%02d:%02d",hour,minute,second)`

2. Input (should not be examined)
  ```java
  System.out.println("Enter a number here: ");
  Scanner in = new Scanner(System.in);
  String s = in.nextLine();  // 读取输入的字符串
  ```

3. String Slice
  Unlike Python, slices of Java string do not support using [0,5] like lists This way.
  - substring()
    ```java
    String original = "Hello, World!";
    
    // 从索引6开始截取到字符串末尾
    String sub1 = original.substring(6); // "World!"
    
    // 截取索引2（包含）到索引5（不包含）之间的字符
    String sub2 = original.substring(2, 5); // "llo"
    
    System.out.println(sub1);
    System.out.println(sub2);
    ```
  - SpringBuilder and charAt()
    ```java
    String original = "Java Programming";
    StringBuilder sb = new StringBuilder();
    // 提取前4个字符
    for (int i = 0; i < 4; i++) {
        sb.append(original.charAt(i));
    }
    String sub = sb.toString(); // "Java"
    System.out.println(sub);
    ```
  
4. Triple expression
  In Java, a three-dollar expression (Ternary Operator) is a simple conditional judgement, also known as a condition operator.
  - Basic grammar:
    ```
    条件表达式 ? 值1 : 值2
    ```
  - Implementation logic
    1. Calculating Conditional Expressions (results must be boolean type)
    2. Return value 1 if condition is true
    3. If condition is false, return value 2
  - Example: Cycle statement when seeking maximum number of conventions (GCD)
    ```java
    for(int i = a>b ? a:b; i>0 ; i--){
        if(a%i==0 && b%i==0){
            GCD_val = i;
            break;
        }
    }
    ```
    Equal to
    ```java
    for(int i=Math.min(a,b); i>=0; i--) {
        if(a%i==0 && b%i==0){
            GCD_val = i;
            break;
        }
    }
    ```
5. Case conversion
  ```java
  // 创建Scanner对象用于从标准输入读取用户输入
  Scanner in = new Scanner(System.in);
  
  // 提示用户输入文本
  System.out.println("Input a text here: ");
  
  // 读取用户输入的整行文本
  String text = in.nextLine();
  
  // 创建StringBuilder对象，用于高效构建结果字符串
  StringBuilder swappedText = new StringBuilder();
  // 遍历输入文本的每个字符
  for(int i = 0; i < text.length(); i++) {
      // 获取当前位置的字符
      char ch = text.charAt(i);
      
      // 判断是否为大写字母
      if(ch >= 'A' && ch <= 'Z') {
          // 将大写字母转换为小写字母
          // 在ch + ('a' - 'A')后，由于加入运算，会自动转为int类型，因此需要(char)进行强制类型转换
          swappedText.append((char)(ch + ('a' - 'A')));
      }
      // 判断是否为小写字母
      else if(ch >= 'a' && ch <= 'z') {
          // 将小写字母转换为大写字母
          swappedText.append((char)(ch - ('a' - 'A')));
      }
      // 非字母字符保持不变
      else {
          swappedText.append(ch);
      }
  }
  
  // 输出转换后的文本
  System.out.println("The swapped text is: " + swappedText);
  ```
6. Infinity and infinity in Java:
  ```java
  double a = Double.POSITIVE_INFINITY;
  double b = Double.NEGATIVE_INFINITY;
  ```


* used in this part (except for special declarations) [signed - non-commercial use - consistent 4.0 International (CC BY-NC-SA 4.0)]https://creativecommons.org/licenses/by-nc-sa/4.0/deed.zh) Licensing agreements. ♪ I'm sorry ♪