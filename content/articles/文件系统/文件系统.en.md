---
title: "Java File Operations and Resource Management"
slug: file-i-o-operations-and-resource-management-using-java-file-class
date: '2025-05-23'
category: Java Fundamentals
draft: false
tags:
- "Java"
- "File System"
---
## Java `File`Category:

### Basic operations:
- Create File Object
  ```java
  File file = new File("blog.md"); // 只是代表一个文件路径的引用，并没有真正在系统中创建这个文件
  ```
  1. Create file objects using relative paths and create files in the system:
    ```java
    File f = new File("test.txt");
    f.creatNewFile(); // 在系统中创建test.txt文件，Java默认创建在项目文件夹的根目录
    System.out.println(f.exists());
    System.out.println(f.getAbsolutePath());
    ```
    Access to:
    ```
    true
    C:\Users\HJ000\IdeaProjects\Pro-25\test.txt
    ```
    ![](https://raw.githubusercontent.com/Ferdinandhu000/my_blog_img/master/20250523091524.png)

  2. Create file objects using absolute paths and create files in the system:
    ```java
    File f = new File("C:\\Users\\HJ000\\IdeaProjects\\Pro-25\\test2.txt");
    f.createNewFile();
    System.out.println(f.exists());
    System.out.println(f.getAbsolutePath());
    ```
    Access to:
    ```
    true
    C:\Users\HJ000\IdeaProjects\Pro-25\test2.txt
    ```
    ![](https://raw.githubusercontent.com/Ferdinandhu000/my_blog_img/master/20250523092414.png)
  
    Besides, it's possible.`../`Format back to the previous directory and access
    ```java
    File f = new File("../test.txt");
    f.creatNewFile(); // 放回上一级目录并创建test.txt文件
    System.out.println(f.exists());
    System.out.println(f.getAbsolutePath());
    ```
    ![](https://raw.githubusercontent.com/Ferdinandhu000/my_blog_img/master/20250523094219.png)

- Create directory object
  ```java
  File dir = new File("newDirectory");
  ```
  1. Create a single layer directory
    ```java
    dir.mkdir();
    ```
  2. Create multilayer directory
    ```java
    dir.mkdirs();
    ```
    ![](https://raw.githubusercontent.com/Ferdinandhu000/my_blog_img/master/20250523092807.png)
  
- Clear Operations
  ```java
  file.delete();  // 删除文件
  dir.delete();   // 删除空目录
  ```

### File Properties Query
- Basic Properties:
  ```java
  file.exists();      // 判断文件是否存在
  file.isFile();      // 判断是否为文件
  file.isDirectory(); // 判断是否为目录
  file.length();      // 获取文件大小（字节）
  file.getName();     // 获取文件名
  file.getPath();     // 获取相对路径
  file.getAbsolutePath(); // 获取绝对路径
  ```
- Relevant competence
  ```java
  file.canRead();     // 是否可读
  file.canWrite();    // 是否可写
  file.canExecute();  // 是否可执行
  file.setReadable(true);    // 设置可读权限
  file.setWritable(true);    // 设置可写权限
  file.setExecutable(true);  // 设置可执行权限
  ```